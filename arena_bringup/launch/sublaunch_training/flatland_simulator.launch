
<launch>
  <arg name="ns"/>
  <!-- the folder name under the path simulator_setup/maps  -->
  <arg name="map_folder_name" default="map_empty" />
  <arg name="world_path" default="$(find simulator_setup)/maps/$(arg map_folder_name)/map.world.yaml" />

  <!-- launch flatland server -->
  <node name="flatland_server" pkg="flatland_server" type="flatland_server" output="screen" ns="$(arg ns)">  
    <!-- remapping for namespace -->
    <param name="world_path" value="$(arg world_path)" />
    <remap from="/clock" to="/$(arg ns)/clock"/>
  </node>
</launch>